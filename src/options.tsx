import React from "react";
import ReactDOM from "react-dom";

const Options = () => {
    return <>Coming Soon</>;
};

ReactDOM.render(
    <React.StrictMode>
        <Options/>
    </React.StrictMode>,
    document.getElementById("root")
);
